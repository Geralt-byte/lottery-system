<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.infrastructure.persistent.dao.IRuleTreeNodeDao">

    <!--    <resultMap id="CaseMap" type="com.xjtu.infrastructure.persistent.po.A">-->
    <!--        <id column="id" property="id"/>-->
    <!--        <result column="create_time" property="createTime"/>-->
    <!--        <result column="update_time" property="updateTime"/>-->
    <!--    </resultMap>-->


    <select id="queryRuleTreeNodeListByTreeId" resultType="com.xjtu.infrastructure.persistent.po.RuleTreeNode">
        select tree_id, rule_key, rule_desc, rule_value
        from rule_tree_node
        where tree_id = #{treeId}
    </select>

    <select id="queryRuleLocks" resultType="com.xjtu.infrastructure.persistent.po.RuleTreeNode">
        select tree_id,rule_value from rule_tree_node
        where rule_key='rule_lock' and tree_id in
        <foreach item="treeId" index="index" collection="array" open="(" separator="," close=")">
            #{treeId}
        </foreach>
    </select>

</mapper>