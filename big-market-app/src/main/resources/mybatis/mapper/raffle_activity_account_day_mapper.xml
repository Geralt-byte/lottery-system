<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.infrastructure.persistent.dao.IRaffleActivityAccountDayDao">

    <insert id="insertActivityAccountDay"
            parameterType="com.xjtu.infrastructure.persistent.po.RaffleActivityAccountDay">
        insert into raffle_activity_account_day(user_id, activity_id, day, day_count, day_count_surplus)
        VALUES (#{userId}, #{activityId}, #{day}, #{dayCount}, #{dayCountSurplus})
    </insert>

    <update id="updateActivityAccountDaySubtractionQuota"
            parameterType="com.xjtu.infrastructure.persistent.po.RaffleActivityAccountDay">
        update raffle_activity_account_day
        set day_count_surplus=day_count_surplus - 1
        where user_id = #{userId}
          and activity_id = #{activityId}
          and day_count_surplus > 0
          and day = #{day}
    </update>

    <select id="queryActivityAccountDayByUserId"
            parameterType="com.xjtu.infrastructure.persistent.po.RaffleActivityAccountDay"
            resultType="com.xjtu.infrastructure.persistent.po.RaffleActivityAccountDay">
        select user_id,
               activity_id,
               day,
               day_count,
               day_count_surplus
        from raffle_activity_account_day
        where user_id = #{userId}
          and activity_id = #{activityId}
          and day = #{day}
    </select>

    <select id="queryRaffleActivityAccountDayPartakeCount" resultType="java.lang.Integer">
        select day_count - day_count_surplus
        from raffle_activity_account_day
        where user_id = #{userId}
          and activity_id = #{activityId}
          and day = #{day}
    </select>
</mapper>