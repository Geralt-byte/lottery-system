<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.infrastructure.persistent.dao.ITaskDao">

    <insert id="insert" parameterType="com.xjtu.infrastructure.persistent.po.Task">
        insert into task(user_id, topic, message_id, message)
        VALUES (#{userId}, #{topic}, #{messageId}, #{message})
    </insert>

    <update id="updateTaskSendMessageCompleted" parameterType="com.xjtu.infrastructure.persistent.po.Task">
        update task
        set state='completed'
        where user_id = #{userId}
          and message_id = #{messageId}
    </update>

    <update id="updateTaskSendMessageFail" parameterType="com.xjtu.infrastructure.persistent.po.Task">
        update task
        set state='fail'
        where user_id = #{userId}
          and message_id = #{messageId}
    </update>

    <select id="queryNoSendMessageTaskList" resultType="com.xjtu.infrastructure.persistent.po.Task">
        select user_id,
               topic,
               message_id,
               message
        from task
        where state = 'fail'
           or (state = 'create' and now() - update_time > 6)
        limit 10
    </select>
</mapper>